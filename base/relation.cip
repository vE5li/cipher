public enumeration Relation {
    Equal
    Bigger
    Smaller
}

implement Relation {

    public label bigger: Function(bigger: Boolean) -> Self := {
        match bigger {
            case true -> return Relation::Bigger
            case false -> return Relation::Smaller
        }
    }

    public label smaller: Function(smaller: Boolean) -> Self := {
        match smaller {
            case true -> return Relation::Smaller
            case false -> return Relation::Bigger
        }
    }
}
